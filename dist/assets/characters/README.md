# Characters (AI / GLB)

Drop your exported character model(s) here.

## Expected starter path

- `public/assets/characters/code-sentinel.glb`

If that file exists, the site will load it via `GLTFLoader`. If not, it will render a procedural fallback “Sentinel” so the experience still runs.

